<Window x:Class="PosAccountingApp.Views.DetailEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="جزئیات و ویرایش"
        Width="600" Height="650"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        Background="#FFF0F4F8"
        Icon="{StaticResource AppIcon}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.10*"/>
            <ColumnDefinition Width="0.80*"/>
            <ColumnDefinition Width="0.10*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" Background="White" CornerRadius="12"
                BorderBrush="#FFE0E0E0" BorderThickness="1"
                VerticalAlignment="Stretch" Margin="0,16">
            <DockPanel Margin="16">
                <!-- Header -->
                <Border DockPanel.Dock="Top" Background="{DynamicResource AccentBrush}"
                        CornerRadius="8" Padding="14,12" Margin="0,0,0,16">
                    <Grid>
                        <TextBlock x:Name="TitleText" Text="جزئیات" FontSize="17" FontWeight="Bold"
                                   Foreground="White"/>
                        <Button Content="&#xE711;" FontFamily="Segoe MDL2 Assets"
                                Background="Transparent" BorderThickness="0"
                                Foreground="White" FontSize="14" HorizontalAlignment="Left"
                                Click="Close_Click"/>
                    </Grid>
                </Border>

                <!-- Fields -->
                <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto" MaxHeight="380">
                    <StackPanel x:Name="FieldsPanel"/>
                </ScrollViewer>

                <!-- Buttons -->
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                            HorizontalAlignment="Center" Margin="0,16,0,0">
                    <Button x:Name="SaveBtn" Content="ذخیره تغییرات" Style="{StaticResource SuccessButton}"
                            Margin="0,0,8,0" Click="Save_Click"/>
                    <Button x:Name="DeleteBtn" Content="حذف" Style="{StaticResource DangerButton}"
                            Margin="0,0,8,0" Click="Delete_Click"/>
                    <Button Content="بستن" Style="{StaticResource OutlineButton}"
                            Click="Close_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
